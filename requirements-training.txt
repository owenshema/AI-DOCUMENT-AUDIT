# Donut Model Training Dependencies
transformers>=4.30.0
datasets>=2.12.0
torch>=2.0.0
torchvision>=0.15.0
torchaudio>=2.0.0
pdf2image>=1.16.3
Pillow>=9.5.0
accelerate>=0.20.0
sentencepiece>=0.1.99

# For PDF conversion (requires poppler)
# Windows: Download poppler from https://github.com/oschwartz10612/poppler-windows/releases/
# Extract and add bin/ folder to PATH
