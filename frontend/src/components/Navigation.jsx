import React from 'react';
import { useLocation } from 'react-router-dom';

// The landing page (/) has its own embedded navbar.
// The app shell pages (dashboard, documents, etc.) have their own sidebar.
// This component renders nothing — navigation is handled per-page.
const Navigation = () => {
  return null;
};

export default Navigation;
